#pragma once

/*! \file versions.hpp
  \brief Packet versions predeclarations.

  All packet versions have to be predeclared in this file, inside packet_version structure
*/

namespace ares {
  struct packet_version {
    struct base {};
  };

}
