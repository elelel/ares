#pragma once

/*! \file packets.hpp
\brief Packet database

This file is the top file to include the packet database hierarchy.
It should contain #include directives to include packet_ids and packets declaration files,
e.g. 
#include "packet_version_name/packet_ids.hpp"
#include "packet_version_name/packets.hpp"
*/

#include <optional>
#include <string>

#include "base/packet_ids.hpp"
#include "base/packets.hpp"
